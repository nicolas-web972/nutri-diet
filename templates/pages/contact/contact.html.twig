{% extends 'base.html.twig' %}

	{% block title %}Nutridiet - Contact
	{% endblock %}

	{% block body %}
		<div class="container">
			{% for message in app.flashes('success') %}
				<div class="alert alert-success mt-4">
					{{ message }}
				</div>
			{% endfor %}
			<h1 class="mt-4">Formulaire de contact</h1>
			{{ form(form) }}
			</div>
		{% endblock %}
